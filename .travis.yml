sudo: required
services:
    -docker


before_install:
    - docker build -t fainy/docker-nginx-react-app -f Dockerfile .

script:
    -docker run  fainy/docker-nginx-react-app npm run test -- --coverage